extends base

block content
	form#translator(action='/translate' method='post')
		span translate
		input#translate-word(type='text' placeholder='word to translate')
		span from
		select#from-language
		span to
		select#to-language
		button#translate-submit(type='submit') translate
	//- todo: pass translation through as variable 
	//- and rerender this page if api request is successful
	h2= translation
	script(src='/js/translate.js')
		